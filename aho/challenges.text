host: root@192.168.1.89
password: "Temp@Pass"
------------

1. **Easy — `/etc/hosts` review and safe edit**
   SSH to `root@192.168.1.89`, inspect `/etc/hosts`, identify duplicate or suspicious entries, make one safe cleanup or comment-only change if warranted, save the final file and notes to `./temp/etc_hosts_review.txt`, and also print a short summary to chat describing what was found and what was changed.

2. **Easy — DNS resolver audit**
   SSH to the host, inspect resolver configuration (`/etc/resolv.conf`, systemd-resolved status if present, and hostname resolution behavior), write the detailed findings to `./temp/dns_audit.txt`, and also print a concise summary to chat showing active DNS servers and any obvious resolver issues.

3. **Easy — `/tmp` and `/var/tmp` cleanup review**
   SSH to the host, inspect disk usage and age of files under `/tmp` and `/var/tmp`, remove only clearly stale safe-to-delete files if appropriate, save the evidence to `./temp/tmp_cleanup.txt`, and also print a summary to chat with before/after usage and what was removed.

4. **Easy — Logrotate configuration check**
   SSH to the host, inspect logrotate configuration and recent rotation status, identify broken or stale rotations if any, save findings to `./temp/logrotate_audit.txt`, and also print a summary to chat highlighting whether log rotation appears healthy.

5. **Easy-Medium — Sudo and privilege audit**
   SSH to the host, inspect `/etc/sudoers` and `sudoers.d` safely, identify overly broad privilege rules or syntax risks, write the audit to `./temp/sudo_audit.txt`, and also print a summary to chat with notable privilege findings.

6. **Easy-Medium — Package cache cleanup and verification**
   SSH to the host, inspect package caches, reclaim safe cache space if the package manager supports it, save the detailed actions and outputs to `./temp/package_cache_cleanup.txt`, and also print a summary to chat with reclaimed space and package-manager health afterward.

7. **Easy-Medium — Service enablement review**
   SSH to the host, list enabled services, identify anything unusual or clearly unnecessary, disable one non-critical service only if it is safe and justified, save the evidence to `./temp/service_enablement_review.txt`, and also print a summary to chat with what was reviewed and changed.

8. **Medium — File permission and ownership audit**
   SSH to the host, audit a target directory such as `/etc`, `/var/www`, or another service directory for risky ownership or world-writable files, write detailed findings to `./temp/permission_audit.txt`, and also print a concise summary to chat with the most important permission issues.

9. **Medium — Broken symlink and stale path audit**
   SSH to the host, search key system paths for broken symlinks and stale references, determine whether they are harmless or risky, write the full report to `./temp/symlink_audit.txt`, and also print a short summary to chat with the most relevant findings.

10. **Medium — Interface and routing change investigation**
    SSH to the host, inspect current network interfaces and routes, compare active configuration files with the live state, identify mismatches or misconfigurations, save findings to `./temp/interface_route_investigation.txt`, and also print a summary to chat with the likely network configuration source of truth.

11. **Medium — Repository configuration repair**
    SSH to the host, inspect package repository configuration files, identify one broken or duplicate repository entry if present, repair it safely, document the steps in `./temp/repo_repair.txt`, and also print a summary to chat with the issue found and final refresh result.

12. **Medium — Journal error trend analysis**
    SSH to the host, review recent journal logs for repeated errors or warnings, group the top recurring patterns, save the detailed analysis to `./temp/journal_error_trends.txt`, and also print a concise summary to chat with the top 3 recurring problems and their likely components.

13. **Medium-Hard — Web/service config syntax validation**
    SSH to the host, identify whether a common service such as nginx, Apache, SSH, or another daemon has editable text config, make one small safe configuration improvement or cleanup if justified, validate syntax, save results to `./temp/service_config_validation.txt`, and also print a summary to chat with the file changed and validation outcome.

14. **Medium-Hard — Port conflict RCA**
    SSH to the host, investigate whether any expected service startup failure or binding issue is due to a port conflict, identify the competing processes, save the RCA to `./temp/port_conflict_rca.txt`, and also print a summary to chat naming the conflicting port, processes, and likely resolution.

15. **Medium-Hard — Package dependency inspection before removal**
    SSH to the host, choose a removable non-critical package, inspect reverse dependencies and what else would be affected, decide whether removal is safe, save the analysis to `./temp/package_dependency_inspection.txt`, and also print a summary to chat with the removal risk assessment.

16. **Hard — Boot-time config and startup delay RCA**
    SSH to the host, inspect boot timing, startup-critical services, and long-running units, determine what is slowing boot or causing degraded startup, save the detailed RCA to `./temp/boot_delay_rca.txt`, and also print a summary to chat with the slowest components and likely root cause.

17. **Hard — Storage inode exhaustion investigation**
    SSH to the host, investigate whether a filesystem is suffering inode exhaustion rather than raw space exhaustion, identify the directories or workloads causing it, save the evidence to `./temp/inode_exhaustion_rca.txt`, and also print a summary to chat with inode usage, culprit paths, and recommended remediation.

18. **Hard — Configuration drift audit**
    SSH to the host, compare current active configs with package defaults or backup copies where available, identify likely configuration drift affecting service behavior, save the findings to `./temp/config_drift_audit.txt`, and also print a summary to chat with the most important drift points.

19. **Hard — Package install/remove change impact RCA**
    SSH to the host, inspect recent package transaction history, determine whether a recent install, upgrade, or removal likely caused a service issue or instability, write the RCA to `./temp/package_change_impact_rca.txt`, and also print a summary to chat with the likely offending package event and supporting evidence.

20. **Hardest — Cross-domain incident triage**
    SSH to the host and investigate a combined complaint such as “network seems odd, disk is filling up, and services restarted after updates” by correlating network state, disk growth, package history, failed units, and recent logs, save the full RCA to `./temp/cross_domain_incident_rca.txt`, and also print an executive summary to chat with the most likely timeline, root cause, confidence, and next steps.

--------------------coding challanges--------------------

1. **Easy** — Build a self-contained Python script at `./temp/csv_column_stats.py` that parses an embedded CSV string, computes per-column counts for numeric and non-empty values, prints a short summary table, and includes built-in `unittest` cases for normal input, empty rows, mixed types, missing fields, and header-only input.

2. **Easy** — Build a self-contained Python script at `./temp/bracket_validator.py` that validates whether strings contain properly balanced `()`, `[]`, and `{}` brackets, prints verdicts for an embedded sample list, and includes built-in `unittest` cases for balanced input, nested brackets, mismatched closers, extra openers, and empty strings.

3. **Easy** — Build a self-contained Python script at `./temp/slugify_titles.py` that converts an embedded list of article titles into lowercase URL slugs with hyphens, strips punctuation, normalizes whitespace, and includes built-in `unittest` cases for punctuation removal, repeated spaces, mixed case, empty strings, and duplicate-title behavior.

4. **Easy** — Build a self-contained Python script at `./temp/windowed_average.py` that computes a moving average over an embedded list of numbers for a configurable window size, prints the resulting sequence, and includes built-in `unittest` cases for window size 1, full-length window, oversized window, negative numbers, and empty input.

5. **Easy-Medium** — Build a self-contained Python script at `./temp/ini_parser.py` that parses an embedded INI-style config string into nested Python dicts, supports sections and key-value pairs, ignores comments and blank lines, and includes built-in `unittest` cases for multiple sections, duplicate keys, malformed lines, comment handling, and empty input.

6. **Easy-Medium** — Build a self-contained Python script at `./temp/markdown_toc.py` that scans an embedded Markdown document, generates a table of contents from `#`, `##`, and `###` headings, prints it in Markdown link form, and includes built-in `unittest` cases for nested headings, duplicate titles, skipped heading levels, special characters, and no-heading input.

7. **Easy-Medium** — Build a self-contained Python script at `./temp/hex_dump_diff.py` that compares two embedded byte strings, prints a compact hex diff showing the first differing offsets, and includes built-in `unittest` cases for identical inputs, single-byte difference, length mismatch, empty inputs, and multiple differences.

8. **Medium** — Build a self-contained Python script at `./temp/sql_where_builder.py` that safely builds a SQL-like `WHERE` clause string from an embedded dict of filters supporting equality, `IN` lists, and `IS NULL`, and includes built-in `unittest` cases for simple equality, multi-value `IN`, null handling, empty filters, and stable key ordering.

9. **Medium** — Build a self-contained Python script at `./temp/mini_template_engine.py` that renders an embedded template string using `{{name}}`-style placeholders from a dict, leaves unknown placeholders unchanged, and includes built-in `unittest` cases for basic substitution, repeated placeholders, missing keys, adjacent placeholders, and empty templates.

10. **Medium** — Build a self-contained Python script at `./temp/lru_cache_sim.py` that implements a tiny LRU cache with `get` and `put`, prints cache state after an embedded operation sequence, and includes built-in `unittest` cases for eviction order, updating existing keys, cache hits, cache misses, and zero-capacity handling.

11. **Medium** — Build a self-contained Python script at `./temp/text_chunker.py` that splits an embedded text into chunks of approximately fixed size with configurable overlap, preserves word boundaries when possible, and includes built-in `unittest` cases for exact-size chunks, overlap behavior, very long words, empty input, and chunk size smaller than overlap.

12. **Medium** — Build a self-contained Python script at `./temp/version_sort.py` that sorts an embedded list of semantic-like version strings such as `1.2.10`, `1.2.3`, and `2.0`, handling missing minor or patch segments sensibly, and includes built-in `unittest` cases for mixed lengths, duplicate versions, leading zeros, invalid tokens, and empty input.

13. **Medium-Hard** — Build a self-contained Python script at `./temp/dag_executor.py` that takes an embedded directed acyclic graph of tasks, computes a valid execution order, and includes built-in `unittest` cases for linear dependencies, branching dependencies, disconnected nodes, cycle detection, and missing-node references.

14. **Medium-Hard** — Build a self-contained Python script at `./temp/regex_router.py` that matches embedded URL paths against a list of regex-based route patterns, returns the first matching handler name and extracted groups, and includes built-in `unittest` cases for exact matches, parameter captures, no match, route precedence, and malformed regex handling.

15. **Medium-Hard** — Build a self-contained Python script at `./temp/undoable_text_editor.py` that simulates a tiny text editor with append, delete, undo, and print operations, outputs results for an embedded command list, and includes built-in `unittest` cases for single undo, chained undos, deleting beyond length, printing empty text, and mixed operations.

16. **Hard** — Build a self-contained Python script at `./temp/constraint_scheduler.py` that schedules embedded tasks with durations, deadlines, and dependency constraints, reports lateness per task, and includes built-in `unittest` cases for on-time schedules, deadline misses, dependency ordering, impossible cycles, and empty task sets.

17. **Hard** — Build a self-contained Python script at `./temp/mini_git_index.py` that tracks an in-memory staging area for file paths and content hashes, supports add, remove, status, and commit snapshot operations, and includes built-in `unittest` cases for new files, modified files, removed files, empty commits, and stable snapshot contents.

18. **Hard** — Build a self-contained Python script at `./temp/rule_engine.py` that evaluates an embedded set of if/then rules against input records, supports comparison and boolean-combination operators, and includes built-in `unittest` cases for single-rule matches, multiple-rule matches, no matches, invalid rule syntax, and missing fields.

19. **Hard** — Build a self-contained Python script at `./temp/merge_intervals_plus.py` that merges overlapping numeric intervals while preserving associated labels when compatible and splitting or rejecting conflicting overlaps when not, and includes built-in `unittest` cases for disjoint intervals, simple merges, nested intervals, conflicting labels, and empty input.

20. **Hardest** — Build a self-contained Python script at `./temp/mini_query_engine.py` that executes a tiny SQL-like query language over an embedded list of Python dict records, supporting `SELECT`, `WHERE`, `ORDER BY`, and `LIMIT`, and includes built-in `unittest` cases for projection, filtering, ordering, limits, malformed queries, and empty datasets.

